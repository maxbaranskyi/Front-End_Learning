import React from 'react';
import ModalWindow from '../common/Modal';

const EditProfile = () => {
    return (
        <ModalWindow>
            <div></div>
        </ModalWindow>
    );
};

export default EditProfile;
